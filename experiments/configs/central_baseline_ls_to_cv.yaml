shared_config:
  eval: en-dev-transf.csv,en-test-transf.csv
  tar: en.tar
  # PUT HERE PATH TO THE MODEL
  restore_checkpoint: pretrained/jax-model
  is_restore_optim: False
central_config:
  train: en-train-transf.csv
  batch_size: 240
server_optim_config:
  warmup: 1
  optim: lars
  lr: 0.5
  lr_decay_start_update: 150000