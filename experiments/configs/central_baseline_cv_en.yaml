shared_config:
  eval: en-dev-transf.csv,en-test-transf.csv
  tar: en.tar
central_config:
  train: en-train-transf.csv
  batch_size: 240
block_config:
  dtype: float32
server_optim_config:
  warmup: 1
  optim: lars
  lr: 0.5
